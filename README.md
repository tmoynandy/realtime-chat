# realtime-chat

Generated by **Helios** — an AI-native backend generator.

> TypeScript · fiber · postgres · redis · REST · railway

## Quickstart

```bash
cp .env.example .env
docker compose up --build
```

## Scripts

| Command | Purpose |
| --- | --- |
| `npm run dev` | Run locally with hot reload |
| `npm run start` | Start the API server |
| `npm test` | Run the test suite |

## Endpoints

| Method | Path | Summary | Auth |
| --- | --- | --- | --- |
| `GET` | `/users/:id` | Get a user by ID | 🔐 |
| `POST` | `/users` | Create a new user | — |
| `POST` | `/auth/login` | Login and issue JWT | — |
| `GET` | `/health` | Liveness probe | — |

## Deploy

Target: **railway** · Region: `us-east-1` · Baseline replicas: 3 (autoscaling enabled).

- Dockerfile and docker-compose at the repo root
- Kubernetes manifests under `deploy/k8s/`
- CI wired for gh-actions at `.github/workflows/ci.yml`

## Observability

grafana is wired in with sensible defaults. OpenTelemetry traces are exported via OTLP.
